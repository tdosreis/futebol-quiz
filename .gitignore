.DS_Store
*.log
node_modules/
.claude/

# --- Android signing & build artifacts (never commit) ---
*.keystore
*.jks
*.aab
*.apk
*.idsig
android/app/build/
android/.gradle/
android/local.properties
android/build/

# the test tools boot a copy of the page with a probe injected; they clean
# up after themselves, but a killed run leaves one behind
_*.html
